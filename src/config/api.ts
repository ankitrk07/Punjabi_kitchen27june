import Constants from "expo-constants";

const getBaseUrl = () => {
  // Use the live hosted Render backend
  return "https://punjabi-kitchen27june.onrender.com/api";
};

export const API_BASE_URL = getBaseUrl();
